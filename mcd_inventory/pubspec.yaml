name: mcd_inventory
description: A Flutter application example demonstrating how a McDonald's-style inventory system might look.
version: 1.0.0

environment:
  sdk: ^3.9.2

flutter:
  uses-material-design: true

dependencies:
  flutter:
    sdk: flutter

  pocketbase: ^0.18.0
  intl: ^0.17.0
  charts_flutter: ^0.12.0
  provider: ^6.0.5
  material_design_icons_flutter: ^6.0.6595

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  test: ^1.25.6
  mockito: ^5.5.0
